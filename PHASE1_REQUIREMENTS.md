# レシピ帳Webアプリ Phase 1 要件定義書

作成日: 2026-08-18
前提: [HANDOFF.md](HANDOFF.md)（Phase 0完了時点の引き継ぎ資料）の内容を前提とする。

Phase 1では以下4機能を実装する。
① 複数端末での同期　② 献立カレンダー　③ レシピデータ拡張　④ 買い物リストの作成

この4つは独立していない（③のデータ拡張が①②④の土台になり、①の同期基盤は②④で作るデータにも及ぶ）ため、
先に全体のデータ・アーキテクチャ方針を決め、そのあと機能ごとの要件を定義する。

---

## 0. 前提として決定した方針（ユーザー確認済み）

| 論点 | 決定 |
|---|---|
| 複数端末同期の実現方法 | 既存のCloudflare Workersアカウントを拡張し、**D1（+ 画像はWorkers KV）** をバックエンドとして追加する |
| 材料の分量(amount) | **数値を基本としつつ、「少々」「適量」等の非数値も許容**するハイブリッド型 |
| 表記ゆれ解消 | **実装時にあらかじめ同義語辞書を用意して組み込む**。それでも解消しない表記ゆれは、**ユーザーが手動で編集**できるようにする（AIによる自動グルーピングは行わない）。**組み込み辞書自体は別セッションで作成する**（[INGREDIENT_ALIAS_PROMPT.md](INGREDIENT_ALIAS_PROMPT.md)参照） |
| 献立の食事区分 | **朝食／昼食／夕食の3種**（間食は無し） |
| 同期コードの共有方法 | **QRコード表示**（+ テキストとしてもコピー可能にする） |
| difficulty（難易度） | **廃止**。レシピデータ拡張の対象から除外する |
| レシピ写真の同期 | アップロード前に**リサイズ・圧縮する前提**で進める |

---

## 1. 全体アーキテクチャの変更

### 1.1 現状（Phase 0）
- バックエンドなし。データはブラウザのIndexedDBのみに保存され、端末ごとに完全に独立。

### 1.2 Phase 1後
- **IndexedDBを「ローカルキャッシュ兼オフライン時の実体」として維持**し、そのままオフラインでも全機能が使えることを維持する。
- 新設する **Cloudflare Workers + D1 + KV** を同期用バックエンドとして追加する。
  - D1: レシピ / フォルダ / 献立カレンダー / 買い物リスト / ユーザー定義エイリアス辞書 のJSONデータを保存
  - KV: レシピ写真（画像バイナリ）を保存。レシピレコード側は画像のKVキーのみを持つ
- **同期コード方式**の簡易認証を採用する（アカウント登録・メールアドレス不要）。
  - 初回、設定画面で「同期を開始する」を押すとランダムな同期コード（例: 12桁の英数字）が発行される。
  - 他の端末では、その同期コードを入力するだけで同じデータ空間に接続できる。
  - **同期コードを知っている人は誰でも読み書きできる**ため、「家族内利用など、信頼できる相手とだけ共有すること」をUI上に明記する。パスワード的な厳格な認証は行わない（要件として過剰なため）。
- **同期タイミング**: データ変更時に自動でバックエンドへプッシュ + アプリ起動時/オンライン復帰時に自動プル。加えて「今すぐ同期」ボタンも設置。
- **競合解決**: レコード単位の `updatedAt` を比較する **Last-Write-Wins**。フィールド単位のマージ等の高度な解決は行わない（Phase 1のスコープ外）。
- **削除の同期**: 物理削除ではなく論理削除（`deletedAt` を立てるtombstone方式）にし、他端末に削除を伝播させたのち、一定期間後に完全削除する。
- **同期対象外**: Gemini APIキー・CORSプロキシURルなど [js/data/settings.js](js/data/settings.js) の端末固有設定は今まで通り同期しない。
- **初回接続時のデータ移行**: 「アップロードか上書きかを選ばせる」方式ではなく、**常にpush→pullの順でLWWマージする**方式で実装した（実装時の設計判断）。ローカルにのみ存在するレコードはサーバーへ追加され、サーバーにのみ存在するレコードはローカルへ追加され、両方に存在するレコードは`updatedAt`が新しい方が残る。追加的なマージになるため、どちらの操作を選んでも既存データが消えることはない。

### 1.3 補足
- 画像はアップロード前にリサイズ・圧縮してからKVへ保存する（KVの1オブジェクト25MB制限、無料枠合計1GBに収める設計とする。具体的な圧縮パラメータは実装時に決定）

---

## 2. 機能① 複数端末での同期（詳細）

### 2.1 対象データ
レシピ、フォルダ、献立カレンダー（②）、買い物リストの保存済み履歴（④）、ユーザー定義の材料名エイリアス辞書（④）

### 2.2 画面・操作
- 設定画面に「同期」セクションを新設
  - 未接続時: 「同期を開始する」ボタン → 同期コード発行（**QRコード表示** + テキストコピー） / 「他の端末のコードで参加する」入力欄（QRコード読み取り、またはテキスト直接入力）
  - 接続済み時: 同期コードのQRコード・テキスト表示、最終同期日時、「今すぐ同期」ボタン、「この端末の同期を解除する」ボタン
- 同期状態（同期中／完了／エラー）をトースト等で軽く表示

### 2.3 非機能要件
- オフライン時も通常通り閲覧・編集可能。オンライン復帰時に自動で差分同期。
- 同期エラー時もローカルデータは保持され、アプリの利用は継続できる。

---

## 3. 機能② 献立カレンダー

### 3.1 データモデル（新規）
```
MealPlanEntry
├─ id
├─ date        // "YYYY-MM-DD"
├─ mealType     // "breakfast" | "lunch" | "dinner"
├─ recipeId     // 既存レシピへの参照
├─ memo         // 任意
├─ createdAt
└─ updatedAt
```
- 1日・1食事区分に複数レシピを割り当て可能（例: 夕食に「主菜」「副菜」を別々に登録）とする。

### 3.2 画面・操作
- グローバルナビゲーションに「カレンダー」を追加（後述 6章のナビゲーション再設計を参照）
- 月表示カレンダーを基本とし、各日にその日の献立（レシピのアイコン/サムネ + 料理名を簡略表示）をプレビュー
- 日付タップで詳細パネルを開き、食事区分ごとに献立レシピの追加・削除ができる
- レシピ追加時は既存レシピ一覧から検索して選択する（カレンダーからの新規レシピ作成はしない）

### 3.3 レシピ削除時の挙動
- レシピを削除する際、そのレシピが献立カレンダーで使用されていれば警告を表示し、確認のうえ削除する。削除確定時は該当する `MealPlanEntry` も連動して削除する。

### 3.4 補足
- 食事区分は**朝食／昼食／夕食の固定3種**とする
- 週表示など月表示以外のビューはPhase 1では用意しない（月表示のみ）

---

## 4. 機能③ レシピデータ拡張

### 4.1 新スキーマと現行フィールドの対応

| 新フィールド | 現行フィールド | 変更内容 |
|---|---|---|
| `id` | `id` | 変更なし |
| `title` | `title` | 変更なし |
| `image` | `photo`（Blob） | リネーム。ローカルはBlobのまま、同期時はKVキー文字列に変換して送受信 |
| `ingredients` | `ingredients` | 下記4.2参照。`optional` を新規追加 |
| `steps` | `steps` | 変更なし |
| `cookingTime` | `cookingMinutes` | リネーム（意味は同じ、分単位） |
| `servings` | （新規） | 人数（数値、任意） |
| `memo` | `memo` | 変更なし |
| `sourceUrl` | `sourceURL` | リネーム（大文字小文字統一） |
| `createdAt` | `createdAt` | 変更なし |
| `updatedAt` | （新規） | レコード更新のたびに更新。①の同期の競合解決に使用 |
| — | `storeName` | 新スキーマに無い項目だが、既存の並び替え機能（店舗名順）等で使用中のため**維持する**（フィールドとして残す） |
| — | `folderId`, `icon` | 新スキーマに無いが機能上必要なため維持する |

### 4.2 Ingredient（新規サブ構造）
```
Ingredient
├─ name
├─ amount    // 数値 or 非数値プリセット文字列（例: "少々" "適量" "お好みで"）
├─ unit
└─ optional  // boolean、省略可の材料かどうか
```
- 分量入力UIは数値入力を基本としつつ、「少々／適量／お好みで／ひとつまみ」等のプリセットを選べるトグルまたはプルダウンを併設する。
- 材料行に「任意（optional）」チェックボックスを追加する。

### 4.3 既存データのマイグレーション
IndexedDBの `DB_VERSION` を上げ、`onupgradeneeded` 相当の処理で既存レコードを自動変換する。
- `cookingMinutes` → `cookingTime`、`sourceURL` → `sourceUrl`、`photo` → `image` にキー名を変換
- 全レコードに `updatedAt` を付与（初期値は `createdAt` と同値）
- 各 `ingredients[].amount` を可能な範囲で数値にパースし、失敗した場合は元の文字列のまま保持（非数値として扱う）
- 既存データを壊さないよう、変換は一度きりのマイグレーションとして実装し、実施後は動作確認を行う

### 4.4 UI変更（編集画面）
- 「人数」の入力欄を新設（[js/ui/edit.js](js/ui/edit.js) / [index.html](index.html) の該当セクション）
- 材料行に分量プリセット選択・任意チェックボックスを追加
- `difficulty`（難易度）は不要と判断し、Phase 1では実装しない

---

## 5. 機能④ 買い物リストの作成

### 5.1 目的
献立カレンダーの期間を指定すると、その期間の全レシピの材料を集計し、チェックリストとして表示する。

### 5.2 表記ゆれ解消の方式（確定）
- 実装時に、一般的な食材の表記ゆれをカバーする**組み込み同義語辞書**を用意する（例: 「じゃがいも」＝「ジャガイモ」＝「馬鈴薯」を同一材料として扱う）。
  - **この組み込み辞書自体の作成は別セッションで行う**。着手用プロンプトを [INGREDIENT_ALIAS_PROMPT.md](INGREDIENT_ALIAS_PROMPT.md) に用意した。本実装セッションでは、そのセッションが出力する辞書データファイル（`js/data/ingredientAliases.js` を想定）を取り込む形で進める。
- 集計時はこの辞書で材料名を正規化してからグルーピングする。
- 辞書でカバーしきれず、本来同じ材料が別々に集計されてしまった場合は、**ユーザーが設定画面等から手動でエイリアス（同義語グループ）を追加・編集**できるようにする。ユーザー定義のエイリアスは組み込み辞書に追加する形で優先適用される。
- ユーザー定義エイリアス辞書は同期対象（①）に含める。

### 5.3 集計ロジック
- 指定期間内の `MealPlanEntry` から対象レシピを重複込みで洗い出し、各レシピの `ingredients` を展開する。
- 材料名を辞書で正規化し、同一材料・同一単位のもの同士は `amount` を合算する。
- `amount` が数値のもの同士のみ合算し、非数値（「少々」等）は合算せず個別に列挙する。
- 単位が異なる場合の自動換算（例: 大さじ→g）は **Phase 1では行わない**。単位が異なる同名材料は別行として表示する。

### 5.4 画面・操作
- グローバルナビゲーションに「買い物リスト」を追加
- 期間指定UI（開始日〜終了日の指定、または「今週」「来週」などのプリセット）
- 集計結果をチェックリスト形式で表示し、チェックで購入済みにできる
- 献立に無い、買い足したい品目を手動で追加できる

### 5.5 データモデル（新規）
```
ShoppingList
├─ id
├─ startDate
├─ endDate
├─ items[]      // { name, amount, unit, checked, manuallyAdded }
├─ createdAt
└─ updatedAt
```
- 生成した買い物リストは履歴として保存し、後から見返せるようにする（都度使い捨てにはしない）。

```
IngredientAlias（ユーザー定義辞書）
├─ id
├─ canonicalName   // 正規化後の代表名（例: "じゃがいも"）
├─ variants[]       // 表記ゆれのバリエーション（例: ["ジャガイモ", "馬鈴薯"]）
├─ createdAt
└─ updatedAt
```

---

## 6. ナビゲーション構成の変更（提案）

現状 [index.html](index.html) はヘッダーの歯車アイコンから設定画面に入る構成のみで、一覧/詳細/編集/設定の4ビューしかない。
機能追加に伴い「カレンダー」「買い物リスト」への導線が必要になるため、**下部タブバー（レシピ一覧／カレンダー／買い物リスト／設定）** の新設を提案する。
（既存のヘッダーの戻る/追加/共有等のアクション構成は維持し、タブバーは大分類の切り替えのみを担う）

---

## 7. 実装順序案

1. **③ レシピデータ拡張**（スキーマ変更・マイグレーション・`updatedAt`付与） — 他機能の土台
2. **① 複数端末同期の基盤**（Cloudflare D1/KVセットアップ、同期コード発行・push/pull・LWW） — データが変わる前に基盤を先に作ることで、二重マイグレーションを避ける
3. **② 献立カレンダー**
4. **④ 買い物リスト**（組み込み同義語辞書 + 集計ロジック + UI）
   - 組み込み同義語辞書（[INGREDIENT_ALIAS_PROMPT.md](INGREDIENT_ALIAS_PROMPT.md)で別セッション作成）は、このステップの着手前までに用意できていればよい（③②と並行して進めても構わない）

各ステップごとに実機（ローカル + 可能ならGitHub Pages）で動作確認しながら進める。

---

## 8. 未確定事項まとめ

すべて解消済み（0章参照）。要件定義は完了とし、次工程（詳細設計・実装）に進める。
