# 着手用プロンプト: 食材名の組み込み同義語辞書の作成

このファイルの内容をそのまま新しいセッションのプロンプトとして貼り付けてください。
（このセッション自体は読み込まれません。以下は独立したセッションが前提知識なしで着手できるよう、必要な文脈をすべて含めています。）

---

## 依頼内容

`C:\Users\skier\クロード\RecipeWebApp\` にある、フレームワークなしの素のHTML/CSS/JS製レシピ帳Webアプリ「RecipeBox」に、
**食材名の表記ゆれを解消するための組み込み同義語辞書**を追加してください。

### 背景・目的
このアプリでは今後、献立カレンダーに登録したレシピをもとに「買い物リスト」を自動生成する機能を実装します。
その際、複数のレシピに登場する同じ食材が、表記の違いによって別々の材料として集計されてしまう問題があります。
（例:「じゃがいも」「ジャガイモ」「馬鈴薯」は同じ食材だが、文字列としては別物として扱われてしまう）

これを解消するため、**あらかじめ一般的な食材の表記ゆれをカバーする辞書データ**を用意します。
この辞書は買い物リスト機能の集計ロジックが、材料名を正規化（代表名に統一）するために使います。
（なお、この辞書だけで解消しきれない表記ゆれは、アプリ側でユーザーが手動編集できるようにする設計になっています。それはこの依頼の対象外です。あなたはあくまで「組み込み辞書の初期データ」の作成に専念してください。）

### 成果物
`C:\Users\skier\クロード\RecipeWebApp\js\data\ingredientAliases.js` という新規ファイルを作成し、以下の形式でデータをエクスポートしてください。

```js
export const BUILTIN_INGREDIENT_ALIASES = [
  {
    canonicalName: 'じゃがいも',        // 正規化後の代表名（買い物リストに表示される名前）
    variants: ['ジャガイモ', '馬鈴薯', 'ばれいしょ', 'ポテト'], // 表記ゆれのバリエーション（canonicalName自体は含めない）
  },
  {
    canonicalName: '玉ねぎ',
    variants: ['玉ネギ', 'たまねぎ', 'タマネギ', 'オニオン'],
  },
  // ...以下同様
];
```

- 1ファイルの単純な配列で構いません（IDやタイムスタンプは不要。ユーザーが編集する辞書とは別物で、こちらは静的な初期データです）。
- 他のアプリコード（`js/main.js` 等）への組み込み・配線は**不要**です。このファイルを新規作成するだけで完結させてください（組み込み作業は別の実装セッションで行います）。
- ファイル冒頭に、簡単な1行コメントで用途を説明してください（多言語対応や表記統一ルールの長い説明文は不要）。

### カバー範囲の目安
まずは日本の家庭料理レシピで頻出する食材を中心に、以下のカテゴリで実用的な件数を収録してください（網羅性より、実際のレシピでよく登場する食材を優先）。

- 野菜・きのこ類（例: じゃがいも、玉ねぎ、にんじん、キャベツ、白菜、ねぎ、しいたけ 等）
- 肉・魚介類（例: 鶏肉/鳥肉、豚肉、牛肉、ひき肉/挽肉、卵/たまご 等の表記ゆれや部位名の略称含む）
- 調味料・基礎食材（例: 醤油/しょうゆ、砂糖、塩、味噌/みそ、酒、みりん、片栗粉、小麦粉、米/お米 等）
- 乳製品・大豆製品（例: 牛乳、バター、豆腐、油揚げ 等）

### 表記ゆれとして統一してよい例・統一してはいけない例
- 統一してよい例: 漢字/ひらがな/カタカナの表記違い（例:「人参」「にんじん」「ニンジン」）、送り仮名の違い、一般的な別名・方言レベルの呼称、単純な省略形
- 統一してはいけない例: 実際には異なる食材・調味料（例:「濃口醤油」と「薄口醤油」は風味も塩分濃度も異なる別物なので統一しない。「豚バラ肉」と「豚ロース肉」も部位が異なるため別物）
  - 迷った場合は「レシピの分量表記として置き換えても違和感がないか」を基準に判断してください

### 制約・注意事項
- 対象は `js/data/ingredientAliases.js` の新規作成のみです。他の既存ファイルは変更しないでください。
- `git`関連の操作（コミット・push等）は行わないでください。ファイル作成のみで完了とします。
- 作業完了後、収録した食材の総数とカテゴリ内訳を簡潔に報告してください。
